package org.example.quizzen.preguntas;

public class PruebaVisualDePregunta {
}

/*
import javafx.application.Application; import javafx.geometry.Insets; import javafx.scene.Scene; import javafx.scene.control.*; import javafx.scene.layout.*; import javafx.scene.text.Font; import javafx.stage.Stage; public class PreguntasApp extends Application { private int preguntaActual = 0; private final String[] preguntas = { "¿Cuál es la capital de Francia?", "¿Cuánto es 2 + 2?", "¿Cuál es el océano más grande?" }; private final String[][] opciones = { {"a) Madrid", "b) París", "c) Roma", "d) Berlín"}, {"a) 3", "b) 4", "c) 5", "d) 22"}, {"a) Atlántico", "b) Índico", "c) Pacífico", "d) Ártico"} }; @Override public void start(Stage stage) { Label titulo = new Label(); titulo.setFont(new Font(22)); VBox opcionesBox = new VBox(10); ToggleGroup grupo = new ToggleGroup(); Button btnAtras = new Button("Atrás"); Button btnSiguiente = new Button("Siguiente"); // Función para cargar una pregunta Runnable cargarPregunta = () -> { titulo.setText(preguntas[preguntaActual]); opcionesBox.getChildren().clear(); grupo.getToggles().clear(); for (String texto : opciones[preguntaActual]) { RadioButton rb = new RadioButton(texto); rb.setFont(new Font(16)); rb.setToggleGroup(grupo); // Evento al seleccionar rb.setOnAction(e -> { for (Toggle t : grupo.getToggles()) { RadioButton r = (RadioButton) t; r.setFont(new Font(16)); // tamaño normal r.setText(r.getText().replace(" ✔", "")); } rb.setFont(new Font(20)); // más grande rb.setText(rb.getText() + " ✔"); // tick }); opcionesBox.getChildren().add(rb); } btnAtras.setDisable(preguntaActual == 0); btnSiguiente.setDisable(preguntaActual == preguntas.length - 1); }; cargarPregunta.run(); btnAtras.setOnAction(e -> { if (preguntaActual > 0) { preguntaActual--; cargarPregunta.run(); } }); btnSiguiente.setOnAction(e -> { if (preguntaActual < preguntas.length - 1) { preguntaActual++; cargarPregunta.run(); } }); HBox botones = new HBox(20, btnAtras, btnSiguiente); VBox root = new VBox(20, titulo, opcionesBox, botones); root.setPadding(new Insets(20)); stage.setScene(new Scene(root, 500, 400)); stage.setTitle("Preguntas"); stage.show(); } public static void main(String[] args) { launch(); } }
 */
